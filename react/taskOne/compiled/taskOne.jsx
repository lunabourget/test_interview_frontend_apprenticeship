import React from 'react';
/**
*  Returns all even numbers from an input array
* @param name
* @return string
*/
const Greeting = ({ name }) => {
  return (
    <h1>Hello, {name}!</h1>
  );
};

export default Greeting;
